{
  "name": "@hart/agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./provider": "./src/provider/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "arena": "tsx scripts/arena.ts",
    "claude-test": "tsx scripts/claude-test.ts",
    "claude-game": "tsx scripts/claude-game.ts"
  },
  "dependencies": {
    "@hart/common": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
